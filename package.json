{
  "name": "apitesting",
  "version": "1.0.0",
  "scripts": {
    "dev": "json-server --watch db.json --port 3000",
    "start": "npx json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
